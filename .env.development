# 开发环境 API 代理目标（Vite proxy 使用）
VITE_API_PROXY_TARGET=http://localhost:8080

# 留空则走 Vite 同源代理 /api
VITE_API_BASE_URL=

# 设为 true 时使用本地 mock，不请求后端
VITE_USE_MOCK=false

# 设为 true 时跳过登录校验（仅本地调试）
VITE_SKIP_AUTH=false
